# Prompt Template：创建配置治理需求卡

```text
我现在新增一个需求：配置中心与敏感配置治理。

请先不要开发，不要修改业务代码，不要操作 Figma，不要执行数据库迁移。

需求背景：
当前项目可能存在配置散落问题，配置可能分布在 JS / Python 文件、local.env、mysql-connection.json、SQLite、MySQL、前端 localStorage、文档或历史代码中。后续产品要升级为“租赁商户运营标准化系统 / 商户 SaaS”，不同商户会有不同配置，因此需要确认当前配置到底存在哪里，并规划后续配置治理方案。

请你作为总控台判断：
1. 这个需求属于哪个阶段；
2. 当前是否允许开发；
3. 如果当前不允许开发，应该放入哪个 Backlog；
4. 需要新增哪些文档；
5. 是否需要更新 Phase 03 的需求文档、Codex 任务和验收标准；
6. 是否需要更新决策记录；
7. 应该交给哪个执行会话后续处理；
8. 请生成一份【总控台调度单】；
9. 请生成需要写入文档的草稿内容。

只输出调度单和文档草稿，不要直接写文件。
```
